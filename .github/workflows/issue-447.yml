name: issue-447

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  job1:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.21.x'
          cache: true
          cache-dependency-path: |
            go.sum 
            go.mod

      - name: Build and Run in List
        run: |
          ls -la
          go build .

  job2:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.21.x'
          cache: true
          cache-dependency-path: "**/*.sum"

      - name: Build and Run in Glob
        run: |
          ls -la
          go build .
  
  job1-1:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.21.x'
          cache: true
          cache-dependency-path: |
            go.sum 
            go.mod

      - name: Build and Run in List2
        run: |
          ls -la
          go build .

  job2-1:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.21.x'
          cache: true
          cache-dependency-path: "**/*.sum"

      - name: Build and Run in Glob2
        run: |
          ls -la
          go build .


  compile_and_test:
    runs-on: ubuntu-latest
    steps:
    - name: Check out code into the workspace
      uses: actions/checkout@v3
      
    - name: Setup go version
      uses: actions/setup-go@v4 # v4.1.0
      with:
        go-version: '1.21.x'
        check-latest: true
        cache: true
        cache-dependency-path: |
            go.sum
            go.mod
      
    - name: Get dependencies
      run: go get -v -t -d ./...
      
    - name: Compile Application
      run: go build -v ./...
      
    - name: Run Tests
      run: go test -v ./...      
