name: Setup GO

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        test: [ '1', '2' ]
    name: Test Job
    steps:
      - name: Setup go
        uses: actions/setup-go@v6
      - run: echo "matrix run ${{ matrix.test }}"
