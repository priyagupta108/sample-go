name: Test Go with self-hosted

on:
  push:
# jobs:
#   test:
#     runs-on: ubuntu-latest
#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v4
  
#     - name: Set up Go
#       uses: nicholasngai/actions-setup-go@download-fallback-dl-google-com-go
#       with:
#         go-version: '1.11.12'
#         # cache: false
        
#     - name: Verify Go version
#       run: go version

#     # - name: Cache Go modules
#     #   uses: actions/cache@v4
#     #   with:
#     #     path: |
#     #       ~/.cache/go-build
#     #       # Cache Go modules
#     #       ~/go/pkg/mod
#     #     key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
#     #     restore-keys: |
#     #       ${{ runner.os }}-go-
#     - name: Install dependencies
#       run: go mod tidy

#     - name: Run tests
#       run: go test ./...
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        test: [ '1', '2' ]
    name: Test Job
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Setup go
        uses: actions/setup-go@v6
      - run: echo "matrix run ${{ matrix.test }}"
